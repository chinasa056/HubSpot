const Booking = require("../models/booking");
const Space = require("../models/space");
const User = require("../models/user");

exports.bookPerHour = async (req, res) => {
    try {
        
    } catch (error) {
        
    }
};